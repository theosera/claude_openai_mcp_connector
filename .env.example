# Absolute path to THIS file. The server reads an env file ONLY from
# MCP_ENV_FILE; it never reads one from its working directory (for a
# client-spawned stdio server that directory is chosen by the client).
# Set it in the real environment (systemd Environment= / launchd
# EnvironmentVariables / an MCP client's env block), not here:
#   MCP_ENV_FILE=/abs/path/to/claude_openai_mcp_connector/.env
# Values already in the real environment always win over this file.

# Local clone path for your private Obsidian/Markdown vault.
# Do not commit a real path or private repository URL.
KNOWLEDGE_ROOT=/path/to/private/obsidian-vault

# Optional: serve SEVERAL roots at once (e.g. the vault + a command-log repo).
# Comma-separated name=path pairs. The FIRST root is the primary (writable);
# every other root is strictly read-only and its documents are addressed as
# "name:relative/path". When set, KNOWLEDGE_ROOTS takes precedence over
# KNOWLEDGE_ROOT. Roots must be disjoint directories.
#KNOWLEDGE_ROOTS=vault=/path/to/private/obsidian-vault,ops=/path/to/ops-log-repo

# --- session-archive hook (Claude Code session history -> vault) ---
# Optional overrides for .claude/skills/session-archive/archive-session.sh.
# By default the hook finds the vault clone by scanning $HOME/*/ for a
# `.claude-session-vault` marker file; unset + no marker = the hook is a no-op.
#SESSION_VAULT_REPO=/path/to/private/obsidian-vault
#SESSION_LOG_SUBDIR=claude-sessions
#SESSION_ARCHIVE_DISABLE=

# Existing files are edited through a plan/apply flow.
MCP_WRITE_MODE=two_step

# Where two-step plan state is written. Plan files hold vault PLAINTEXT (the
# pre-edit text and the full proposed text) until they are applied; they are
# git-ignored local state. Left unset it defaults to ~/.mcp-state/patches-<hash>,
# derived from this vault's path so two servers started against different vaults
# never share a plan directory (a plan is looked up by patch_id alone). It
# does not move with the caller — and if no home directory is available to derive
# that from (an empty or relative HOME, e.g. under a service account or a
# --clearenv sandbox) the server exits rather than falling back to the working
# directory, so set this explicitly there. If you do set it, note that a
# RELATIVE value resolves against the process working directory, NOT against the
# file named by MCP_ENV_FILE — and for a client-spawned stdio server that
# directory is chosen by the client. Use an absolute path.
#MCP_PATCH_STATE_DIR=/abs/path/to/claude_openai_mcp_connector/.mcp-state/patches

# Max Markdown files opened at once during a vault scan. Bounds file-descriptor
# pressure on large vaults (a naive all-at-once open can surface as transient
# EAGAIN/EMFILE, esp. on iCloud/network-backed folders). Default 24 if unset.
#MCP_SCAN_CONCURRENCY=24

# Ceiling on the parse cache, in CHARACTERS of retained text — the body of every
# cached note plus the two derived copies the search path needs, so about three
# times the body and counted in UTF-16 characters, not bytes on disk. Default
# 192,000,000, which holds a vault of roughly 116 MB of source text.
#
# This is a safety valve for a vault far larger than that, NOT a memory knob:
# measured on a 2,894-note vault, moving this from 1M to 192M characters changed
# retained heap by 1.9 MB. What it does control is whether the cache works at
# all — every read path enumerates the whole vault, so a budget below one sweep
# evicts the sweep's own front before the next sweep reaches it and every query
# re-parses (measured at that size: 91 ms -> 689 ms). Raise it if the first
# eviction warning appears on stderr.
#MCP_DOCUMENT_CACHE_MAX_CHARS=192000000

# How strongly a recent note is favoured in search ranking. OFF by default (0):
# leaving these unset changes no ranking, so an upgrade is never a surprise.
# 0.25 is the recommended starting point — enough to break ties toward current
# work, not enough to bury an older note that is a better textual match. The
# boost is multiplicative, so it only re-orders notes that already matched; it
# can never surface one that did not. A single request can override it with
# `recency_weight`, and `order: "recent"` sorts by date regardless of this.
#MCP_SEARCH_RECENCY_WEIGHT=0.25
# Days after which the boost halves (exponential decay). Default 30.
#MCP_SEARCH_RECENCY_HALFLIFE_DAYS=30

# Optional constrained destination for instruction-only Skill bundles. This is
# relative to the primary knowledge root and must already exist. Existing Skills
# are never overwritten.
#
# Setting this RESERVES the subtree against general document writes (INV-8). It
# does NOT hand this process the tools that write into it — see the stdio flags
# below. Set it on every write-capable process; that is what the reservation is
# for.
#MCP_SKILLS_SUBDIR=path/to/skills

# Optional vault subtree reserved for an audit/scan write surface. Relative to the
# primary knowledge root and must already exist. When set, GENERAL document writes
# can never target it (only append_audit_report / compare_and_swap_audit_state
# write here). Keep it disjoint from "projects/". Example: 90_Audit/vault-scan.
#
# Same split as above: this reserves, it does not grant.
#MCP_AUDIT_SUBDIR=path/to/audit-subtree

# --- stdio write surfaces (both default OFF) ---
# Reserving a subtree and registering the tools that write into it are separate
# decisions, so each surface needs its own opt-in on stdio, exactly as it does on
# HTTP. Leave these unset for any session you type into: stdio reads untrusted
# vault content, an audit write is a single call with no plan/apply step, and a
# Skill is loaded by later sessions as INSTRUCTIONS.
#
# Setting either without its subdir above is a contradiction and refuses to boot.
#MCP_STDIO_ALLOW_SKILL_WRITE=1
#MCP_STDIO_ALLOW_AUDIT_WRITE=1

# --- Remote HTTP transport (for ChatGPT / Claude.ai Chat connectors) ---
# Select transport: "stdio" (default, local CLI clients) or "http" (remote Chat).
MCP_TRANSPORT=stdio

# Bearer secret every HTTP request must present. REQUIRED when MCP_TRANSPORT=http.
# Generate a long random value; never commit the real token.
MCP_AUTH_TOKEN=replace-with-a-long-random-secret

# HTTP bind host/port. Default binds to loopback only; expose via a tunnel.
MCP_HTTP_HOST=127.0.0.1
MCP_HTTP_PORT=8787

# Read-only by default. Set to 1/true to also expose write tools over HTTP.
MCP_HTTP_ALLOW_WRITE=

# The legacy one-step create_document. OFF by default on BOTH transports (stdio
# included), because it is the only document write with no plan/apply pair: the
# vault changes on the tool call itself, before any diff has been shown. Prefer
# plan_document_create -> apply_planned_document_create. Set to 1/true only if
# you still rely on the routed projects/<client>/<project>/<slug>.md capture.
# Has no effect unless writes are enabled for the transport in use.
MCP_ALLOW_LEGACY_CREATE_DOCUMENT=

# Independently expose only the constrained Skill plan/apply tools over HTTP.
# Requires MCP_SKILLS_SUBDIR and does not expose general document write tools.
MCP_HTTP_ALLOW_SKILL_WRITE=

# Independently expose ONLY the constrained audit tools (append_audit_report /
# compare_and_swap_audit_state) over HTTP. Requires MCP_AUDIT_SUBDIR and does not
# expose general document write tools. Set this WITHOUT MCP_HTTP_ALLOW_WRITE to
# run a read-only-plus-audit "scan" endpoint an unattended scanner connects to.
MCP_HTTP_ALLOW_AUDIT_WRITE=

# DNS-rebinding protection. Comma-separated. Defaults to host:port + localhost:port.
# When tunneling, add your public hostname, e.g. vault.example.com.
# Compared as HOSTNAMES: any ":port" suffix is ignored, so "vault.example.com"
# and "vault.example.com:8787" are equivalent. IPv6 literals may be written with
# or without brackets. Leave empty for the default.
MCP_HTTP_ALLOWED_HOSTS=
# Comma-separated FULL origins (scheme included), e.g. https://chat.example.com.
# Compared exactly. Empty = the Origin check is skipped; when set, only a
# present-but-unlisted Origin is refused (a request with none still passes,
# because non-browser MCP clients do not send one).
MCP_HTTP_ALLOWED_ORIGINS=

# Optional public base for ChatGPT citation URLs (else vault:// is used).
MCP_HTTP_PUBLIC_URL=

# --- OAuth 2.1 authorization server (for ChatGPT / Claude.ai web) ---
# ChatGPT/Claude.ai web cannot send a static bearer; enable OAuth for them.
MCP_OAUTH_ENABLED=
# Required when OAuth is enabled: the public HTTPS issuer URL (your tunnel URL).
# MCP_HTTP_PUBLIC_URL is reused as the OAuth issuer (set it above).
# Shared login password gating vault access at the OAuth consent screen.
MCP_OAUTH_PASSWORD=replace-with-a-vault-login-password
# Token lifetimes (seconds). Sensible defaults if unset.
MCP_OAUTH_ACCESS_TTL=3600
MCP_OAUTH_REFRESH_TTL=2592000
MCP_OAUTH_CODE_TTL=60
# Optional: persist OAuth sessions across server restarts (off = in-memory,
# every restart forces a re-authorize). Points at a state FILE the server
# creates (0600). Tokens are stored as sha256 hashes (nothing recoverable) and
# the file is integrity-protected with a key derived from MCP_OAUTH_PASSWORD —
# rotating the password revokes all persisted sessions.
# MCP_OAUTH_STATE_FILE=/abs/path/to/state/oauth-state.json

# --- Context engineering (get_context / get_project_state) ---
# Both are read once by loadConfig() and apply to stdio and HTTP alike.

# Optional owner-controlled document-type weighting for get_context.
# Unset (the default) means every document weighs 1.0 and ranking is
# byte-identical to not having the feature.
#
# Three things fail the boot rather than degrading quietly:
#   - a relative path (must be absolute)
#   - a path that resolves inside ANY knowledge root; symlinks are followed
#     component by component, so pointing outside and linking back in is
#     refused too
#   - a file that cannot be read, is not JSON, or does not validate
# The middle one is the security-shaped rule: a root is synced and writable — by
# Obsidian peers, by imported notes, by the MCP write tools — so a ranking file
# inside one is a ranking that anything able to write a note gets to edit.
# The third one is deliberate as well: a setting you configured and silently did
# not get would leave you reading a ranking that is not the one you set.
#
# Schema, worked example, and the 1.25 ceiling on self-declared signals:
# README.md -> "Document-type weighting for get_context (optional)".
#MCP_CONTEXT_TYPE_RULES=/absolute/path/outside/the/vault/rules.json

# Frontmatter tag marking a note as a project's designated state document.
# get_project_state returns those notes in full, against its token_budget.
# Default: project-state. Empty or whitespace-only falls back to that default.
#
# This is the only seat for a conclusion. The response's `summary` field is
# derived metadata only (doc_count / latest_ts / roots — src/projectState.ts);
# there is no synthesized prose and no `blockers` / `next_steps` field, so
# whatever a human or an offline pipeline concluded has to live in a note
# carrying this tag, which the tool then returns verbatim.
#
# A note qualifies only when its frontmatter carries BOTH `project: <name>` and
# this tag, so a clipping cannot reach state_docs on the tag alone.
#MCP_PROJECT_STATE_TAG=project-state
